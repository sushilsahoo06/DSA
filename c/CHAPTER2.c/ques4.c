#include<stdio.h>
#include<conio.h>

int main(){
    int x;
    printf("enter a number =");
    scanf("%d",x);
    if(x<0){
        x= x * (-1)
        printf("the absolute value is = %d",x);
    }
    
    getch();
}